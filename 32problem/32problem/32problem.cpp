#include<iostream>
using namespace std;
int main()
{
	int num1, num2 , result;
	cout << "Enter the two numbers to start\n";
	cin >> num1 >> num2;
	result = (num1 ^ num2);
	cout << "SUM=\t" << result << endl;
	 
	//^ convert to binary the add A^B=A with binary +B with binary;
	//pow(base ,power) ;
	
	
}